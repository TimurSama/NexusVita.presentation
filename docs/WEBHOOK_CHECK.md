# Проверка Webhook для Telegram бота

## Критически важно!

Бот **НЕ БУДЕТ РАБОТАТЬ** без правильно настроенного webhook!

## Шаг 1: Проверьте webhook в BotFather

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду: `/getwebhookinfo`
3. Должен быть показан URL: `https://etholife.vercel.app/api/telegram/webhook`

### Если webhook не настроен или неправильный:

```
/deletewebhook
/setwebhook
https://etholife.vercel.app/api/telegram/webhook
```

**Важно:** После `/setwebhook` BotFather должен ответить, что webhook установлен.

## Шаг 2: Проверьте endpoint webhook-info

Откройте в браузере:
```
https://etholife.vercel.app/api/telegram/webhook-info
```

Должен вернуть JSON с информацией:
- `success: true`
- `webhookInfo` с URL вашего webhook
- `botInfo` с информацией о боте

Если возвращает ошибку - проверьте переменные окружения.

## Шаг 3: Проверьте логи в Vercel

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите проект `etholife`
3. Перейдите в **Deployments**
4. Откройте последний деплой
5. Перейдите в **Functions** → `api/telegram/webhook` → **Logs**
6. Отправьте `/start` боту
7. Проверьте логи - должны быть записи:
   - `=== WEBHOOK CALLED ===`
   - `🚀 /start command received from:`
   - `✅ Update processed successfully`

### Если в логах НЕТ записей:

- Webhook не настроен или неправильный URL
- Проверьте `/getwebhookinfo` в BotFather
- Переустановите webhook

### Если в логах ЕСТЬ записи, но бот не отвечает:

- Проверьте ошибки в логах
- Убедитесь, что `TELEGRAM_BOT_TOKEN` правильный
- Проверьте, что база данных доступна

## Шаг 4: Проверьте переменные окружения

В Vercel Dashboard → Settings → Environment Variables:

- ✅ `TELEGRAM_BOT_TOKEN` - должен быть установлен
- ✅ `DATABASE_URL` - должен быть установлен

**Важно:** После добавления переменных нужно **пересобрать проект**!

## Типичные проблемы:

### 1. Webhook не получает обновления

**Причина:** Webhook не настроен или неправильный URL

**Решение:**
```
/deletewebhook
/setwebhook
https://etholife.vercel.app/api/telegram/webhook
```

### 2. Бот инициализируется, но не отвечает

**Причина:** Ошибка в обработке обновлений

**Решение:** Проверьте логи в Vercel на ошибки

### 3. "TELEGRAM_BOT_TOKEN not set"

**Причина:** Переменная окружения не установлена

**Решение:** Добавьте `TELEGRAM_BOT_TOKEN` в Vercel Environment Variables и пересоберите проект

## Быстрая диагностика:

1. ✅ Проверьте webhook: `/getwebhookinfo` в BotFather
2. ✅ Проверьте endpoint: `https://etholife.vercel.app/api/telegram/webhook-info`
3. ✅ Проверьте логи: Vercel Dashboard → Functions → Logs
4. ✅ Отправьте `/start` боту и проверьте логи

## Если ничего не помогает:

1. Удалите webhook: `/deletewebhook` в BotFather
2. Пересоберите проект в Vercel
3. Установите webhook заново: `/setwebhook https://etholife.vercel.app/api/telegram/webhook`
4. Проверьте логи после отправки `/start`
