# Настройка Telegram бота на Render

## Текущая конфигурация

Бот настроен на использование **polling** (long polling), что идеально для Render, так как это долгоживущий процесс.

## Шаг 1: Удалить webhook (если был установлен)

Если ранее бот был подключен к Vercel через webhook, нужно его удалить:

```bash
# Замените YOUR_BOT_TOKEN на ваш токен бота
curl -X POST "https://api.telegram.org/botYOUR_BOT_TOKEN/deleteWebhook"
```

Или через браузер:
```
https://api.telegram.org/botYOUR_BOT_TOKEN/deleteWebhook
```

Проверьте, что webhook удален:
```
https://api.telegram.org/botYOUR_BOT_TOKEN/getWebhookInfo
```

Должно вернуть: `"url": ""`

## Шаг 2: Проверить настройки на Render

1. **Переменные окружения** должны быть установлены:
   - `TELEGRAM_BOT_TOKEN` - токен вашего бота
   - `DATABASE_URL` или `POSTGRES_URL` - строка подключения к PostgreSQL
   - `NODE_ENV=production`
   - `RENDER=1` (опционально, но полезно для keep-alive)

2. **Build Command**: `pnpm install --frozen-lockfile && pnpm run build:bot`
3. **Start Command**: `pnpm run start:bot`

## Шаг 3: Проверить работу бота

1. **Проверьте логи в Render Dashboard**:
   - Должны быть сообщения: `✅ Database initialized`, `✅ Telegram bot started`
   - Не должно быть ошибок подключения

2. **Проверьте health endpoint**:
   ```
   https://ethoslife-telegram-bot.onrender.com/health
   ```
   Должен вернуть: `{"status":"ok","service":"telegram-bot","timestamp":"..."}`

3. **Отправьте /start боту в Telegram**:
   - Бот должен ответить приветствием
   - Если не отвечает, проверьте логи на Render

## Шаг 4: Устранение проблем

### Бот не отвечает

1. **Проверьте логи Render**:
   - Откройте Render Dashboard → ваш сервис → Logs
   - Ищите ошибки подключения к базе данных или Telegram API

2. **Проверьте переменные окружения**:
   - `TELEGRAM_BOT_TOKEN` должен быть установлен
   - `DATABASE_URL` должен быть валидным

3. **Проверьте, что webhook удален**:
   ```bash
   curl https://api.telegram.org/botYOUR_BOT_TOKEN/getWebhookInfo
   ```

### Бот отвечает с задержкой

- На бесплатном плане Render сервис может засыпать после 15 минут неактивности
- Keep-alive ping настроен автоматически (каждые 5 минут)
- Для стабильной работы рекомендуется использовать платный план

### Ошибки подключения к базе данных

- Убедитесь, что `DATABASE_URL` правильный
- Проверьте, что база данных доступна из Render
- Проверьте SSL настройки в строке подключения

## Проверка статуса

После успешного деплоя вы должны увидеть в логах:

```
🤖 Starting Telegram Bot Server...
✅ Database initialized
✅ Maria profile initialized
✅ Telegram bot started
🚀 Bot server running on port 3001
📡 Health check: http://localhost:3001/health
Telegram bot initialized
Telegram bot started
```

## Команды бота

После подключения бот поддерживает следующие команды:
- `/start` - приветствие и регистрация
- `/menu` - главное меню
- `/today` - план на сегодня
- `/note [текст]` - добавить заметку

## Важно

- **Polling vs Webhook**: Бот использует polling, поэтому webhook НЕ нужен
- **Keep-alive**: Автоматически настроен для предотвращения засыпания
- **База данных**: Используется та же база, что и для Vercel (PostgreSQL)
