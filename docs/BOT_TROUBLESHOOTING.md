# Устранение проблем с Telegram ботом

## Бот не отвечает на /start

### Шаг 1: Проверьте webhook

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте: `/getwebhookinfo`
3. Должен быть показан URL: `https://your-project.vercel.app/api/telegram/webhook`

Если webhook не настроен или неправильный:

```
/deletewebhook
/setwebhook
https://your-project.vercel.app/api/telegram/webhook
```

### Шаг 2: Проверьте логи в Vercel

1. Откройте Vercel Dashboard
2. Перейдите в Deployments
3. Откройте последний деплой
4. Перейдите в Functions → `api/telegram/webhook` → Logs
5. Отправьте `/start` боту
6. Проверьте логи - должны быть записи:
   - `=== WEBHOOK CALLED ===`
   - `🚀 /start command received from:`
   - `✅ Update processed successfully`

### Шаг 3: Проверьте переменные окружения

В Vercel Dashboard → Settings → Environment Variables:

- ✅ `TELEGRAM_BOT_TOKEN` - должен быть установлен
- ✅ `DATABASE_URL` - должен быть установлен

### Шаг 4: Проверьте endpoint webhook-info

Откройте в браузере:
```
https://your-project.vercel.app/api/telegram/webhook-info
```

Должен вернуть информацию о webhook и боте.

## Если webhook не получает обновления

1. Проверьте, что URL правильный (без лишних слэшей)
2. Проверьте, что проект задеплоен
3. Попробуйте переустановить webhook:
   ```
   /deletewebhook
   /setwebhook
   https://your-project.vercel.app/api/telegram/webhook
   ```
4. Проверьте, что бот не заблокирован
5. Убедитесь, что вы отправляете команду правильному боту

## Персональные приветствия

Бот автоматически распознает пользователей:
- `403161451` - Мария (персональное приветствие)
- `7694835964` - Tixy (персональное приветствие)
- `8530599793` - Запасной аккаунт (персональное приветствие)
- Остальные - стандартное приветствие

## Доступные команды

- `/start` - Начать работу с ботом
- `/menu` - Меню быстрого доступа
- `/today` - План на сегодня
- `/help` - Справка по командам
- `/settings` - Настройки уведомлений

## Быстрая диагностика

1. Проверьте webhook: `/getwebhookinfo` в BotFather
2. Проверьте логи: Vercel Dashboard → Functions → Logs
3. Проверьте endpoint: `/api/telegram/webhook-info`
4. Проверьте переменные: Vercel Dashboard → Environment Variables
