# Быстрая настройка базы данных для Vercel

## ⚡ Самый быстрый способ: Vercel Postgres (5 минут)

### Шаг 1: Создать базу данных в Vercel

1. Откройте ваш проект на [Vercel Dashboard](https://vercel.com/dashboard)
2. Перейдите в раздел **Storage**
3. Нажмите **Create Database** → выберите **Postgres**
4. Выберите регион (ближайший к вам)
5. Нажмите **Create**

### Шаг 2: Получить строку подключения

После создания базы данных:
1. В разделе Storage найдите вашу базу данных
2. Откройте вкладку **.env.local**
3. Скопируйте значение переменной `POSTGRES_URL` или `DATABASE_URL`

### Шаг 3: Добавить переменную окружения

1. В настройках проекта Vercel перейдите в **Settings** → **Environment Variables**
2. Добавьте переменную:
   - **Name**: `DATABASE_URL`
   - **Value**: вставьте скопированную строку подключения
   - **Environment**: выберите Production, Preview, Development (или все)
3. Нажмите **Save**

### Шаг 4: Передеплой

1. Перейдите в раздел **Deployments**
2. Нажмите на последний деплой → **Redeploy**
3. Или сделайте новый коммит в репозиторий

### Готово! ✅

База данных автоматически создастся при первом запросе к API. Профиль Марии будет создан автоматически.

---

## Альтернатива: Supabase (тоже быстро, ~5 минут)

### Шаг 1: Создать проект Supabase

1. Перейдите на [supabase.com](https://supabase.com)
2. Войдите или зарегистрируйтесь (бесплатно)
3. Нажмите **New Project**
4. Заполните:
   - **Name**: `nexusvita` (или любое имя)
   - **Database Password**: придумайте надежный пароль (сохраните его!)
   - **Region**: выберите ближайший
5. Нажмите **Create new project**

### Шаг 2: Получить строку подключения

1. В проекте Supabase перейдите в **Settings** → **Database**
2. Найдите раздел **Connection string**
3. Выберите **URI** формат
4. Скопируйте строку подключения (она выглядит как `postgresql://postgres:[YOUR-PASSWORD]@...`)

### Шаг 3: Добавить в Vercel

1. В настройках проекта Vercel: **Settings** → **Environment Variables**
2. Добавьте:
   - **Name**: `DATABASE_URL`
   - **Value**: вставьте строку подключения из Supabase
   - **Environment**: все окружения
3. Нажмите **Save**

### Шаг 4: Передеплой

Сделайте redeploy проекта на Vercel.

---

## 🔐 Автоматический вход через Telegram

**Да, вход работает автоматически!** 

Когда пользователь открывает приложение в Telegram:
1. Telegram Web App автоматически передает данные пользователя
2. Приложение вызывает `/api/auth/telegram-auth`
3. Если пользователь с таким Telegram ID существует - он входит автоматически
4. Если не существует - создается новый аккаунт

**Для Марии:**
- Telegram ID: `403161451`
- Профиль создается автоматически при первом запуске сервера
- План упражнений генерируется автоматически

---

## 🧪 Проверка работы

После настройки базы данных:

1. Откройте ваше приложение на Vercel
2. Войдите через Telegram
3. Проверьте, что:
   - Профиль загружается
   - План упражнений отображается
   - Данные сохраняются

---

## ❓ Частые вопросы

### SQLite vs Postgres

- **SQLite**: работает только локально, не работает на Vercel
- **Postgres**: работает везде, включая Vercel

### Нужно ли настраивать Supabase отдельно?

Да, Supabase - это отдельный сервис. Но настройка занимает 5 минут и делается один раз.

### Vercel Postgres vs Supabase

- **Vercel Postgres**: встроенная интеграция, проще настроить
- **Supabase**: больше функций (авторизация, storage, realtime), бесплатный тариф щедрее

### Автоматический вход в Telegram

Да, работает автоматически! Telegram Web App передает данные пользователя, и приложение автоматически создает/находит аккаунт.

---

## 📝 Что уже настроено в коде

✅ Код автоматически определяет окружение:
- Локально → использует SQLite
- На Vercel → использует Postgres (если есть `DATABASE_URL`)

✅ Все API endpoints обновлены для async работы

✅ Профиль Марии создается автоматически при запуске

✅ Telegram авторизация работает автоматически

---

## 🚀 Следующие шаги

1. Настройте базу данных (выберите Vercel Postgres или Supabase)
2. Добавьте `DATABASE_URL` в Environment Variables
3. Сделайте redeploy
4. Откройте приложение в Telegram и проверьте работу!
