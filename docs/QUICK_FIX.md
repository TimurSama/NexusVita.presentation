# БЫСТРОЕ РЕШЕНИЕ: Бот не отвечает

## Проблема: Бот молчит, не отвечает на /start

## Решение за 3 шага:

### Шаг 1: Установите webhook (ОБЯЗАТЕЛЬНО!)

Откройте в браузере (замените `YOUR_BOT_TOKEN` на ваш токен из BotFather):
```
https://api.telegram.org/botYOUR_BOT_TOKEN/setWebhook?url=https://etholife.vercel.app/api/telegram/webhook
```

**Как получить токен:**
1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте: `/mybots`
3. Выберите вашего бота
4. Нажмите "API Token"
5. Скопируйте токен

**Проверка:**
Должен вернуться: `{"ok":true,"result":true,"description":"Webhook was set"}`

### Шаг 2: Проверьте, что webhook установлен

Откройте в браузере (замените `YOUR_BOT_TOKEN`):
```
https://api.telegram.org/botYOUR_BOT_TOKEN/getWebhookInfo
```

Должен показать URL: `https://etholife.vercel.app/api/telegram/webhook`

### Шаг 3: Проверьте логи

1. Откройте Vercel Dashboard → Deployments → последний деплой
2. Перейдите в Functions → `api/telegram/webhook` → Logs
3. Отправьте `/start` боту
4. Должны появиться записи:
   - `=== WEBHOOK CALLED ===`
   - `🚀 /start command received from:`

**Если записей НЕТ** → webhook не настроен, вернитесь к шагу 1

**Если записи ЕСТЬ, но бот не отвечает** → проверьте ошибки в логах

## Диагностика:

Откройте в браузере:
```
https://etholife.vercel.app/api/telegram/debug
```

Для теста подключения к базе данных:
```
https://etholife.vercel.app/api/telegram/debug?test=true
```

Покажет:
- Установлен ли токен
- Настроена ли база данных
- Все endpoints
- Статус подключения к БД (если test=true)

## Если все еще не работает:

1. Убедитесь, что `TELEGRAM_BOT_TOKEN` добавлен в Vercel Environment Variables
2. Убедитесь, что проект пересобран после добавления переменной
3. Проверьте, что используете правильный токен бота
4. Проверьте логи на ошибки
