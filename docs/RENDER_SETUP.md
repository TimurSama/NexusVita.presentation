# Настройка Telegram бота на Render

## Шаг 1: Подготовка репозитория

✅ Все файлы уже готовы:
- `server/bot-server.ts` - сервер для бота
- `render.yaml` - конфигурация Render
- `package.json` - обновлен с командами для бота

## Шаг 2: Создание сервиса на Render

1. **Войдите в Render Dashboard**: https://dashboard.render.com

2. **Создайте новый Web Service**:
   - Нажмите "New +" → "Web Service"
   - Выберите "Build and deploy from a Git repository"
   - Подключите ваш GitHub репозиторий: `TimurSama/NexusVita.presentation`

3. **Настройте сервис**:
   - **Name**: `ethoslife-telegram-bot`
   - **Region**: Выберите ближайший (например, Frankfurt)
   - **Branch**: `main`
   - **Root Directory**: оставьте пустым (корень репозитория)
   - **Runtime**: `Node`
   - **Build Command**: `pnpm install && pnpm run build:bot`
   - **Start Command**: `pnpm run start:bot`
   - **Plan**: `Free` (бесплатный)

4. **Добавьте переменные окружения**:
   - Нажмите "Environment" в настройках сервиса
   - Добавьте:
     - `NODE_ENV` = `production`
     - `TELEGRAM_BOT_TOKEN` = ваш токен бота (из BotFather)
     - `DATABASE_URL` = строка подключения к PostgreSQL (та же, что в Vercel)
     - `POSTGRES_URL` = та же строка подключения (если используется)

5. **Сохраните и деплойте**:
   - Нажмите "Create Web Service"
   - Render автоматически начнет деплой

## Шаг 3: Настройка Webhook (опционально)

После деплоя у вас будет URL вида: `https://ethoslife-telegram-bot.onrender.com`

### Вариант 1: Использовать Polling (рекомендуется для бесплатного плана)
Бот автоматически использует polling (long polling) - ничего настраивать не нужно!

### Вариант 2: Использовать Webhook
Если хотите использовать webhook:

1. Откройте в браузере (замените `YOUR_BOT_TOKEN` и `YOUR_RENDER_URL`):
   ```
   https://api.telegram.org/botYOUR_BOT_TOKEN/setWebhook?url=https://YOUR_RENDER_URL/webhook
   ```

2. Проверьте webhook:
   ```
   https://api.telegram.org/botYOUR_BOT_TOKEN/getWebhookInfo
   ```

## Шаг 4: Отключение бота от Vercel

### 4.1. Удалите webhook с Vercel

1. Откройте в браузере (замените `YOUR_BOT_TOKEN`):
   ```
   https://api.telegram.org/botYOUR_BOT_TOKEN/deleteWebhook
   ```

2. Или установите пустой webhook:
   ```
   https://api.telegram.org/botYOUR_BOT_TOKEN/setWebhook?url=
   ```

3. Проверьте, что webhook удален:
   ```
   https://api.telegram.org/botYOUR_BOT_TOKEN/getWebhookInfo
   ```
   Должен вернуть: `{"ok":true,"result":{"url":"","has_custom_certificate":false,"pending_update_count":0}}`

### 4.2. (Опционально) Удалите webhook endpoint из Vercel

Если хотите полностью удалить бота из Vercel:
- Файлы в `api/telegram/` можно оставить (они не будут использоваться)
- Или удалите их, если хотите очистить проект

## Шаг 5: Проверка работы

1. **Проверьте логи в Render**:
   - Render Dashboard → ваш сервис → "Logs"
   - Должны быть записи:
     - `✅ Database initialized`
     - `✅ Telegram bot started`
     - `🚀 Bot server running on port 3001`

2. **Проверьте health check**:
   - Откройте: `https://YOUR_RENDER_URL/health`
   - Должен вернуть: `{"status":"ok","service":"telegram-bot",...}`

3. **Проверьте бота**:
   - Отправьте `/start` боту в Telegram
   - Бот должен ответить

## Важные замечания о бесплатном плане Render

⚠️ **Бесплатный план Render**:
- Сервис **засыпает после 15 минут неактивности**
- При первом запросе после сна есть задержка "холодного старта" (30-60 секунд)
- Для бота это не проблема, так как он использует polling и автоматически "просыпается"

✅ **Решение**: Бот настроен с keep-alive ping каждые 5 минут, чтобы предотвратить засыпание

## Troubleshooting

### Бот не отвечает
1. Проверьте логи в Render Dashboard
2. Проверьте, что `TELEGRAM_BOT_TOKEN` установлен правильно
3. Проверьте, что `DATABASE_URL` установлен правильно

### Ошибка подключения к базе данных
1. Убедитесь, что `DATABASE_URL` правильный
2. Проверьте, что база данных доступна из интернета (не только локально)

### Бот засыпает
- Это нормально для бесплатного плана
- Бот автоматически проснется при следующем сообщении
- Или используйте keep-alive сервис (например, UptimeRobot) для пинга `/health` каждые 5 минут

## Миграция на платный план (опционально)

Если нужна стабильная работа без засыпания:
- Render Starter: $7/мес - сервис не засыпает
- Или используйте Railway ($5/мес) или DigitalOcean ($5/мес)
